# Resend API key (https://resend.com). If missing, leads are stored but no email is sent.
RESEND_API_KEY=
# Verified sender. Use a domain verified in Resend, or onboarding@resend.dev for testing.
FROM_EMAIL=FQ <hola@tudominio.com>
